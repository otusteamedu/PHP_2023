upstream balancer-nginx {
		least_conn;
		server otus-nginx:80;
		server otus-nginx2:80;
}

server {
    listen 80;
    server_name application.local;

    location / {
       	proxy_pass http://balancer-nginx;
    }
}
