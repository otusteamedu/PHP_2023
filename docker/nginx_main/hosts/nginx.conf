http {
    upstream nginx {
        server application-nginx-1:80;
        server application-nginx-2:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://nginx;
        }
    }
}

events { }
