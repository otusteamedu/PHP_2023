upstream nginx-main {
 least_conn;
 server nginx-slave-1:8000;
 server nginx-slave-2:8000;
}

server {
 listen 80;

 server_name application.local;

 location / {
     proxy_pass http://nginx-main/;
 }
}