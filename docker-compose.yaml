version: '3'

services:
  client:
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: myapp/php
    container_name: app1
    volumes:
       - ./code:/data/
    networks:
      - app-network

  server:
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: myapp/php
    container_name: app2
    volumes:
      - ./code:/data/
    networks:
      - app-network

networks:
  app-network:
    driver: bridge