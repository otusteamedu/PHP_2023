upstream server-balancer {
    least_conn;
    server server-1:8080;
    server server-2:8080;
}

server {
    listen 80;

    server_name application.local;

    location / {
        proxy_pass http://server-balancer/;
    }
}
