version: '3'
services:
  otus_hw6_server:
    build:
      context: ./docker/php
      dockerfile: php.dockerfile
    volumes:
      - ./:/var/www/html
    networks:
      - app-network
networks:
  app-network:
    driver: bridge
