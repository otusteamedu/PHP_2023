version: '3.0'

services:

  # php-cli service
  otus-cli:
    build:
      context: ./cli
      dockerfile: Dockerfile
    image: cli
    container_name: otus-cli
    volumes:
      - ./www:/www
    networks:
      - otus-network

networks:
  otus-network:
    driver: bridge