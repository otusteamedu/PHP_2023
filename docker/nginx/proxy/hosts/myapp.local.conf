upstream app-webservers {
    least_conn;
    server webserver1;
    server webserver2;
}

server {
    listen 80;

    server_name myapp.local;

    location / {
        proxy_pass http://app-webservers;
    }
}