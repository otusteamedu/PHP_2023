name: Deploy

on:
  push:
    branches:
      - NDybnov/hw21

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: http://5.35.98.239
    steps:
      - name: deploy
        run: |
          echo '..deploy...'
