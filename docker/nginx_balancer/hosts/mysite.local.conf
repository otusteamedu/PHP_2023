upstream balancer.app {
    server nginx_container_1;
    server nginx_container_2;
}

server {
    listen 80;

    server_name mysite.local;

    location / {
        proxy_pass http://balancer.app;

        }
    }