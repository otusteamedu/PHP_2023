version: '3'
services:
  server:
    build:
      context: ./docker/php
      dockerfile: php.dockerfile
    volumes:
      - ./:/var/www/html
      - php-fpm-socket:/sockets/php-fpm
    networks:
      - app-network
  client:
    build:
      context: ./docker/php
      dockerfile: php.dockerfile
    volumes:
      - ./:/var/www/html
      - php-fpm-socket:/sockets/php-fpm
    networks:
      - app-network
networks:
  app-network:
    driver: bridge
volumes:
  php-fpm-socket:
