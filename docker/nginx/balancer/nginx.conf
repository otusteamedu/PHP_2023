events {
    worker_connections 1024;
}

http {
    upstream web-servers {
        least_conn;
        server nginx1;
        server nginx2;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://web-servers;
        }
    }
}