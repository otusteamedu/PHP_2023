version: '3.0'

services:
  webserver:
    build:
      context: ./_docker/nginx
      dockerfile: Dockerfile
 #   image: nginx:latest
    volumes:
       - ./code/:/var/www/
    ports:
      - 666:80
    container_name: webserver

