version: '3'

services:
  deploy-php-cli:
#    image: php:8.3.1-cli-alpine
    build:
      context: ./.docker/php-cli/
      dockerfile: Dockerfile
    image: image-deploy-php-cli
    container_name: deploy-php-cli
    volumes:
      - ./:/app
    ports:
      - ${OWN_NGINX_PORT}:8000
#    command: php -S 0.0.0.0:8000 -t /app
