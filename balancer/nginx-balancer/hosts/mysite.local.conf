upstream mysite.local {
    least_conn;
    server nginx-slave1;
    server nginx-slave2;
}

server {
    listen 80;
    server_name mysite.local;

    location / {
        proxy_pass http://mysite.local;
    }
}
