version: '3'

services:
  deploy-php-cli:
    build:
      context: ./.docker/php-cli/
      dockerfile: Dockerfile
    image: image-deploy-php-cli
    container_name: deploy-php-cli
    volumes:
      - ./:/app
    ports:
      - ${OWN_NGINX_PORT}:8000
