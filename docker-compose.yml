version: '3.5'
services:
  php-cli:
    profiles: [ "services" ]
    build:
      context: ${PHP_CLI_CONTEXT}
    container_name: ${SHORT_NAME}-php-cli
    working_dir: ${WORK_DIR}
    tty: true
    volumes:
      - ${APPLICATION_ROOT}