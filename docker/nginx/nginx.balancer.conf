upstream nginx-workers {
    server nginx-worker1:80;
    server nginx-worker2:80;
}

server {
    listen 80;
    server_name default_server;
    location / {
        proxy_pass http://nginx-workers;
    }
}